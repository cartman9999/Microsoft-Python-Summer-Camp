# arrays
# tipos de datos simples como numeros
# todos deben tener el mismo tipo de dato
from array import array

scores = array('d')
scores.append(97)
scores.append(99)

print(scores)
print(scores[1])